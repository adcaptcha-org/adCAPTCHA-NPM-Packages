# adCAPTCHA NPM Node package example 

## Available Scripts

In the project directory, you can run:

```bash
npm start
```

#### NOTE: `API_KEY` has to be set in your `.env.local` and The node NPM package has to be built running `npm run build`.
